# Optional: enables fresh story generation through Groq on the server.
# Never expose this value to the browser or commit a real .env file.
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Railway provides PORT automatically; local default is 3000.
PORT=3000
